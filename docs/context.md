# Context

## Project

- Name: `mlable`
- Focus: TensorFlow utilities for layers, model blocks, metrics, sampling, schedules, and shaping helpers.
- Packaging: generic project (`pyproject.toml`) targeting Python `>=3.10,<4.0`.
